# JSON Header Guidance

JSON does not support comments.

Do **not** add a copyright header as a JSON property because that changes the data schema.

For JSON files:

- Use repository-level `LICENSE` and `NOTICE` files.
- Document licensing at the repository/component level where necessary.
- Do not add artificial `copyright` or `license` JSON fields unless they are explicitly part of the defined schema.
